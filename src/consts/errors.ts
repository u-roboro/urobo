export enum ErrorName {
  REQUIRED = 'required',
  MIN_LENGTH = 'minLength',
  MAX_LENGTH = 'maxLength',
  TYPE = 'type',
  EMAIL = 'email',
}

export enum ErrorTypeValidation {
  REQUIRED = 'required',
  EMAIL = 'email',
  TYPE = 'typeError',
  MIN = 'min',
  MAX = 'max',
}
